version: "3.8"

services:
  app:
    build: .
    image: chit-chat
    container_name: chit-chat_c
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: ["npm", "run", "dev"]
